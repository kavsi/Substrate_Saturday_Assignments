//Array problem solution
fn main() {
    // Fix the error
    let _arr = [1, 2, 3];  // no. 3 is removed from single quotes as array accepts similar type variables

    println!("Success!");
}
