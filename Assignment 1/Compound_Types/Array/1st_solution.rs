//Array problem solution
fn main() {
    // Fill the blank with proper array type
    let arr: [i32;5] = [1, 2, 3, 4, 5];   // the array type and number a re written in format of '[type;no. of elements]'

    // Modify the code below to make it work
    assert!(arr.len() == 5);              // length of array is 5

    println!("Success!");
}
