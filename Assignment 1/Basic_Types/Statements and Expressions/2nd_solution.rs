//Statements and Expressions Exercises solution
fn main() {
   let v = {
   let x = 3;   // changed closed brackets to curly brackets and added return value
   x
   };

   assert!(v == 3);

   println!("Success!");
}