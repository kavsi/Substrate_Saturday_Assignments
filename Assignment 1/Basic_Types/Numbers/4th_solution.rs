//Integer problem solution
fn main() {
    assert_eq!(i8::MAX, 127);  // i8 can store numbers from -(2^7) to 2^7 - 1 and MAX is 127
    assert_eq!(u8::MAX, 255);  // u8 can store numbers from 0 to 2^8 - 1 and MAX is 255.

    println!("Success!");
}
